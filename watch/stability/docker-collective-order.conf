[Unit]
# Give the CIFS mount a chance to complete before Docker restores containers
# that bind-mount /mnt/collective. Wants preserves the existing nofail policy:
# Docker may still start if the remote storage server is unavailable.
Wants=mnt-collective.mount
After=mnt-collective.mount
